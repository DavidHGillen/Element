/**
 * Core low level class, represents all the data associated with an edge. Edges are non rendered information, and all
 * their information will probably simply determine properties elsewhere.
 */
class Edge extends Evee {
	// ctor
	////////////////////////////////////////////////////////////////////////////
	constructor() {
		super();

		this._startStack = null;
		this._endStack = null;
		this._faces = [];
	}

	// core
	////////////////////////////////////////////////////////////////////////////
}
